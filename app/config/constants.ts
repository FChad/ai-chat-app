/** Maximum image upload size in bytes (20 MB) */
export const MAX_IMAGE_SIZE = 20 * 1024 * 1024

/** Threshold in px from the bottom to consider the user "at bottom" */
export const SCROLL_BOTTOM_THRESHOLD = 10

/** Debounce delay in ms for localStorage saves during streaming */
export const SAVE_DEBOUNCE_MS = 500

/** Default OpenRouter model — free router that auto-selects a free backend model */
export const DEFAULT_MODEL = 'openrouter/free'
